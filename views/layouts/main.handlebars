<!DOCTYPE html>
<html lang="en">
    <head>
	{{>head}}
		<title>Lansite</title>
    </head>

    <body>
        <!-- Actual Page -->
        <div class="container">
			{{>navbar}}
            <div id="stream">
				{{{body}}}
            </div>
            <footer class="footer">
                <p>Lansite by Tanner Krewson - <a href="https://github.com/tannerkrewson/lansite">View on Github</a></p>
            </footer>
        </div> <!-- /container -->
    {{>foot}}
    {{>DefaultBoxTemplates}}
	{{>CustomBoxTemplates}}
	</body>
</html>
